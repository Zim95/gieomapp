import React from 'react';

const filter = (props) => {
    return (
        <span>
            <i></i>
            <span><strong>Filter</strong></span>
        </span>
    );
};

export default filter;